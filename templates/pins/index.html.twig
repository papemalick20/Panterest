{% extends 'base.html.twig' %}

{% block title 'Find all the best pins earth' %}

{% block body %}
 <h1>{{ pluralize(pins|length, 'Pin')}} </h1>
 {% for pin in pins %}
   <article>
    <h1> {{pin.title}}</h1>
    <p> {{pin.description}}</p>
   </article>
   {% endfor %}
{% endblock %}
